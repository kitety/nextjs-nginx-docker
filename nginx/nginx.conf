worker_processes  1;

events {
  worker_connections  1024;
}

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;
  sendfile      on;
  keepalive_timeout  65;

  server {
    listen 3000;

    # 反向代理外部 API：/myapi/* -> https://jsonplaceholder.typicode.com/*
    location /myapi/ {
      proxy_ssl_server_name on;
      proxy_set_header Host jsonplaceholder.typicode.com;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      # 例：/myapi/todos/1 -> /todos/1
      proxy_pass https://jsonplaceholder.typicode.com/;
    }

    # 其余请求转发给 Next.js standalone（容器内 3001）
    location / {
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
      proxy_set_header Host $host;

      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      proxy_pass http://127.0.0.1:3001;
    }
  }
}

